name: ADB Software Solutions CI

on:
    push:
        branches: [main]
        paths: &paths
            - "sites/adb-software-solutions/**"
            - "packages/**"
            - "Dockerfile.adb-software-solutions"
            - "package.json"
            - "pnpm-workspace.yaml"
            - "pnpm-lock.yaml"
            - ".github/workflows/adb-software-solutions-ci.yml"
    pull_request:
        types: [opened, synchronize, reopened, ready_for_review]
        paths: *paths
    workflow_dispatch:

concurrency:
    group: adb-software-solutions-ci-${{ github.ref }}
    cancel-in-progress: true

jobs:
    frontend:
        if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
        permissions:
            contents: read
            id-token: write
        uses: adb-software-solutions/github-actions-workflows/.github/workflows/frontend-ci.yml@v1
        with:
            pnpm-version: 10.15.0
            working-directory: sites/adb-software-solutions
            lockfile: pnpm-lock.yaml
            test-command: pnpm test --passWithNoTests

    trivy:
        if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
        permissions:
            contents: read
        uses: adb-software-solutions/github-actions-workflows/.github/workflows/trivy-fs.yml@v1
        with:
            scan-path: sites/adb-software-solutions
            artifact-name: trivy-fs-scan-adb-software-solutions

    build:
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        needs: [frontend, trivy]
        permissions:
            contents: read
            id-token: write
        uses: adb-software-solutions/github-actions-workflows/.github/workflows/docker-build-push.yml@v1
        with:
            application: adb-web-platform
            images: '["adb-software-solutions"]'
            infisical-identity-id: 199f317a-d030-4f03-bbd7-ebf986030b09
            infisical-registry-project-id: 848009f4-aa7d-45e2-8fb6-404bd8f25115
            infisical-application-project-id: 51df9308-efb3-45b0-94ed-bf641f076acf
            build-env-file: sites/adb-software-solutions/.ci.env
            optional-build-env-keys: '["NEXT_PUBLIC_GTM_ID","NEXT_PUBLIC_API_URL"]'
