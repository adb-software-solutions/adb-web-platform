import { Container } from "@/components/ui";
import { CredentialHealthWorkspace } from "./CredentialHealthWorkspace";

export default function CredentialHealthPage() {
    return (
        <Container className="py-8">
            <CredentialHealthWorkspace />
        </Container>
    );
}
